**粒子群算法 (PSO) 的概念与使用**

* **核心概念**：模拟鸟群觅食行为的群智能优化算法。每个候选解被称为“粒子”，粒子在解空间中飞行，通过追踪自身历史最优解（$pbest$）和群体全局最优解（$gbest$）来动态更新自己的飞行速度和位置。
* **应用场景**：适用于多维连续变量的非线性优化问题。在无人机烟幕遮蔽模型中，用于全局粗搜索，能迅速在庞大的空间中锁定烟幕弹最佳爆炸坐标和时间的初始优良区域。



**差分进化算法 (DE) 的概念与使用**

* **核心概念**：基于种群差异的全局优化算法。通过对种群中随机选择的个体进行“变异”（利用个体间的向量差）、交叉操作生成新个体，再通过贪婪准则保留更优解。
* **应用场景**：擅长处理复杂的非线性、多峰值优化问题。在PSO算法后期容易发生粒子聚集、丧失多样性时，引入DE进行精细化局部搜索，有效跳出局部最优陷阱。



**其他常见的数学建模算法**

* **图论与网络算法**：如 Dijkstra、Floyd 最短路算法，以及 DAG（有向无环图）的拓扑排序、环检测与连通性验证（如利用 NetworkX 处理任务路径或依赖关系）。


* **经典运筹学规划**：线性规划 (LP)、混合整数线性规划 (MILP)、非线性规划，适用于资源分配、静态选址和确定性规划。
* **元启发式算法**：遗传算法 (GA) 和模拟退火算法 (SA)，常用于解决复杂的组合优化和调度问题。
* **计算几何与插值算法**：如 de Casteljau 算法，用于计算和评估高阶 Bézier 曲线，常用于无人机平滑轨迹生成与拟合。



**代码实现与调优中的常见问题**

* **约束处理与罚函数设计**：如多枚烟幕弹投放必须间隔 $\ge 1s$ 的硬约束，在代码中若直接作为边界条件困难时，通常采用“罚函数法”——对违反约束的解赋予一个极大的适应度惩罚值，迫使算法自动淘汰非法解。


* **维数灾难与耦合降维**：多机多弹联合优化时决策变量激增，代码中若采用全剧本强耦合直接求解极易不收敛。通常需要采用“分步优化”策略，先固定历史云团叠加效应，再逐个求解单机策略。


* **参数敏感性调优**：PSO 的惯性权重 $\omega$ 和加速常数 $c_1, c_2$ 若设置不当，会导致算法在后期震荡无法收敛，通常需要设计随迭代次数动态衰减的惯性权重策略。

---

**算法复杂度与维度灾难**

* *评委可能问：* 随着无人机数量和烟幕弹枚数的增加（如多机多弹协同），决策变量呈线性甚至指数级增长，算法的**时间复杂度**是如何变化的？
* *计算机视角回答：* 决策变量维度 $D$ 增加会直接导致粒子群或差分进化算法的搜索空间呈指数级膨胀。我们在代码中通过分步优化和空间解耦（如先确定无人机轨迹再优化起爆点）将高维全局优化转化为低维局部迭代，将复杂度有效降维，避免了严重的维度灾难。

**适应度函数的性能瓶颈与并行化**

* *评委可能问：* 启发式算法在每一次迭代中都需要对大量粒子计算适应度（即模拟导弹飞行与烟幕遮蔽），代码运行会不会很慢？你是怎么做性能优化的？
* *计算机视角回答：* 适应度函数中包含了大量的空间几何求交和采样点判断（如对圆柱体表面均匀采样判断视线夹角）。纯 Python 的 `for` 循环会导致严重的性能瓶颈。我们使用了 **NumPy 向量化矩阵运算**替代显式循环，利用底层 C 语言加速大规模数据计算；针对复杂的迭代评估，也可以通过 `multiprocessing` 模块实现多进程并行评估粒子。



**目标函数的非线性与数值不稳定性**

* *评委可能问：* 烟幕遮蔽时长对时间和坐标的导数是否连续？如果目标函数存在大量“断层”（例如刚好遮蔽与差一点没遮蔽的阶跃），启发式算法该如何应对？
* *计算机视角回答：* 遮蔽判定本质上是一个离散的逻辑判断（采样点是否在视线锥体内），直接求导非常困难，属于典型的**非凸、非光滑优化问题**。因此我们无法使用梯度下降法，必须依赖无导数的启发式算法（PSO/DE）。同时，在设计适应度函数时，引入了渐进式的平滑过渡或惩罚项，避免目标函数出现绝对的“零梯度”死区，保证算法能持续获得搜索方向。



**空间几何与碰撞检测的高效实现**

* *评委可能问：* 导弹与烟幕弹球体、无人机轨迹的实时相交判断，在计算机底层是怎么高效实现的？
* *计算机视角回答：* 采用了类似包围盒（Bounding Volume）的筛选思想：先通过简单的几何距离公式计算导弹到烟幕球心的包围范围进行快速初筛（剔除绝对不相交的无效计算），再对通过初筛的目标圆柱体表面进行高精度的采样点夹角计算，减少不必要的浮点运算开销。

